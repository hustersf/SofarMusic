<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sf.libplayer">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application>

        <activity
            android:name=".test.google.camera2.CameraActivity"
            android:label="@string/app_name"></activity>

        <activity
            android:name=".test.other.encode.EncodeActivity"
            android:label="@string/app_name"></activity>


        <activity
            android:name=".test.other.decode.DecodeActivity"
            android:label="@string/app_name"></activity>

        <activity
            android:name=".test.other.decode.LocalH264Activity"
            android:label="@string/app_name"></activity>
    </application>
</manifest>


