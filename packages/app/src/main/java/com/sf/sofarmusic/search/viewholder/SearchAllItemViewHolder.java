package com.sf.sofarmusic.search.viewholder;

import android.view.View;
import com.sf.sofarmusic.search.model.SearchAllInfo;
import com.sf.widget.recyclerview.MultiRecyclerViewHolder;

public class SearchAllItemViewHolder extends MultiRecyclerViewHolder<SearchAllInfo> {

  public SearchAllItemViewHolder(View itemView) {
    super(itemView);
  }

  @Override
  protected void onCreateView(View itemView) {
    super.onCreateView(itemView);
  }

  @Override
  protected void onBindData(SearchAllInfo item) {

  }
}
